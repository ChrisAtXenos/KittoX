DisplayLabel: %Config:AppTitle% %APP_VERSION%
Controller: BorderPanel
  DisplayMode: FullScreen
  WestView:
    DisplayLabel: Main Menu
    Controller: BorderPanel
      Width: 230
      Collapsible: True
      Border: True
      Split: True
      Header: True
      NorthView:
        Controller: HtmlPanel
          Split: False
          Html: <center><p><img src="%IMAGE(Firebird_Employee)%" width="150" height="47"></img></p></center>
      CenterView:
        Controller: TreePanel
          TreeView: MainMenu
      # User-selectable theme switcher at the bottom of the left menu panel
      # (Light / Auto / Dark). Renders only when Theme: Auto /
      # UserSelection: True; otherwise the band collapses.
      SouthView:
        Height: 44
        Controller: ThemeSwitcher

  CenterView:
    Controller: TabPanel
      Border: True
      SubViews:
        View: Customers
        View: Employees

  NorthView:
    Controller: ToolBar
      TreeView: MainMenu

  # Status bar as a full-width bottom band (moved here from the menu's south
  # so the menu south can host the theme switcher, matching TasKitto).
  SouthView:
    Controller: StatusBar
      Text: |
        Connected to: <b>EMPLOYEE</b> &nbsp;&mdash;&nbsp; User: <b>%Auth:UserName%</b> &nbsp;&mdash;&nbsp; FullName: <b>%Auth:FULL_NAME%</b> &nbsp;&mdash;&nbsp; <b>EMail: %Auth:EMAIL_ADDRESS%</b> - Authenticated by: <b>%Auth:LDAP_HOST%</b>
      ImageName: person
