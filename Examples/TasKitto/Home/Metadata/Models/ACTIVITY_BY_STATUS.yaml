Fields:
  STATUS: String(12) primary key
    DisplayLabel: _(Status)
    DisplayWidth: 12
  ACTIVITY_COUNT: Integer
    DisplayLabel: _(Count)
  TOTAL_HOURS: Decimal(9, 2)
    DisplayLabel: _(Hours)
PhysicalName: VW_ACTIVITY_BY_STATUS
# Backed by a SQL view: read-only (no REST write endpoints).
IsReadOnly: True
ImageName: checklist
